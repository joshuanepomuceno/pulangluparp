# Job-specific recipes must only be reachable through the authenticated
# REST endpoint (plct/v1/recipes), never by requesting this file directly.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
